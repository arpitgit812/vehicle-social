<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vehicle Social</title>
  <!-- Include Bootstrap CSS (Add the correct path based on your setup) -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f3f3f3;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      margin: 20px auto;
      background-color: #fff;
      border: 1px solid #ddd;
      border-radius: 5px;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
      padding: 20px;
    }

    .navbar {
      background-color: #3498db;
      padding: 15px 0;
    }

    .navbar a {
      color: #fff;
      margin: 0 10px;
      text-decoration: none;
    }

    .post {
      border: 1px solid #ddd;
      border-radius: 5px;
      margin-bottom: 20px;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
    }

    .post img {
      width: 100%;
      height: auto;
      border-radius: 5px;
      margin-bottom: 15px;
    }
    .navbar-dark .navbar-nav .nav-link {
      color: white!important;
    }
    a:hover, a:active {
      background-color: green;
      color: white;
    }

    .button-container {
      display: flex;
      justify-content: space-between;
    }

    .button-container button {
      border: none;
      border-radius: 3px;
      padding: 10px;
      cursor: pointer;
      font-size: 16px;
    }

    .button-container button.like-button {
      background-color: #3498db;
      color: #fff;
    }

    .button-container button.liked-button {
      background-color: #e74c3c;
      color: #fff;
    }

    .button-container button.comment-button,
    .button-container button.read-comments-button,
    .button-container button.share-button {
      background-color: #2ecc71;
      color: #fff;
    }
    .footer {
      background-color: #3498db;
      color: #fff;
      text-align: center;
      padding: 10px;
      position: realtive;
      bottom: 0;
      width: 100%;
    }

    .footer a {
      color: #fff;
      margin: 0 10px;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <!-- Navigation Bar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="#">Vehicle Social</a>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <%= link_to 'New Post', new_post_path, class: 'nav-link' %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'nav-link', data: { turbolinks: false } %>
          </li>
        <% else %>
          <span class=nav-link>Dont Have Account=></span>
        <li class="nav-item">
            <%= link_to 'Sign Up', new_user_registration_path, class: 'nav-link signup_btn' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Login', new_user_session_path, class: 'nav-link login_btn' %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>

  <div class="container">
    <h1 class="mb-4">Latest Posts</h1>
    
    <% @posts.each do |post| %>
      <div class="post">
        <h2><%= link_to post.title, post %></h2>
        <p><%= post.content %></p>
        <% if post.image.attached? %>
          <%= image_tag post.image, class: "img" %>
        <% end %>
        <div class="button-container">
          <% if current_user && post.likes.exists?(user_id: current_user.id) %>
            <%= button_to "Unlike", destroy_like_post_path(post, post.likes.find_by(user_id: current_user.id), post_id: post.id), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn liked-button", remote: true %>
          <% else %>
            <%= button_to "Like", create_likes_post_path(post), method: :post, data: { confirm: 'Are you sure?' }, class: "btn like-button", remote: true %>
          <% end %>
          <%= button_to "Comment", new_post_comment_path(post_id: post.id), method: :get, class: "btn comment-button" %>
          <%= button_to "Read Comments", post_comments_path(post), method: :get, class: "btn read-comments-button" %>
          <%= button_to "Share (#{post.share_count})", share_post_path(post), method: :post, class: "btn share-button" %>
        </div>
      </div>
    <% end %>
  </div>

  <div class="footer">
    <p>&copy; <%= Time.now.year %> Vehicle Social</p>
    <ul>
      <li><%= link_to 'Home', '#home' %></li>
      <li><%= link_to 'About Us', '#about' %></li>
      <li><%= link_to 'Contact', '#contact' %></li>
    </ul>
  </div>

  <!-- Include Bootstrap JS (Add the correct path based on your setup) -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.7.0/dist/js/bootstrap.min.js"></script>

  <!-- Include your existing JavaScript code -->
  <script>
    document.addEventListener('ajax:success', function(event) {
      var button = event.target.querySelector('.like-button, .liked-button');
      var response = event.detail[0];

      if (button) {
        if (response.liked) {
          button.innerText = 'Unlike';
          button.classList.remove('like-button');
          button.classList.add('liked-button');
        } else {
          button.innerText = 'Like';
          button.classList.remove('liked-button');
          button.classList.add('like-button');
        }
      }
    });
  </script>
</body>
</html>
